{
  "tools": [
    {
      "name": "read-file",
      "description": "Read a file",
      "inputSchema": {
        "type": "object",
        "properties": { "path": { "type": "string" } },
        "required": ["path"]
      }
    },
    {
      "name": "read file",
      "description": "A colliding sanitized name",
      "inputSchema": { "type": "object", "properties": {} }
    }
  ]
}
